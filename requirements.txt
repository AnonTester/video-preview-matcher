ImageHash>=4.3.1
Pillow>=10.0.0
numpy>=1.26.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
tqdm>=4.66.0

# System (non-pip) dependencies required:
#   ffmpeg / ffprobe   - video probing, scene extraction, frame dumping
#   chromaprint (fpcalc) - audio fingerprinting
#
# Debian/Ubuntu:
#   sudo apt install ffmpeg chromaprint
#
# macOS:
#   brew install ffmpeg chromaprint
